{
  "name": "meeting-note-formatter-skill",
  "version": "1.1.0",
  "description": "將逐字稿或會議紀錄整理為繁體中文 Markdown 的 Gemini Interactions API skill",
  "type": "module",
  "scripts": {
    "build": "bun build src/format.js --outfile scripts/format.js --target node --minify",
    "format": "node scripts/format.js"
  },
  "dependencies": {
    "@google/genai": ">=1.33.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
